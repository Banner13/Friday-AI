services:    
  kyuz0-vllm:
    image: kyuz0/vllm-therock-gfx1151:latest

    # amd gpu
    runtime: amd
    environment:
      - AMD_VISIBLE_DEVICES=all

    shm_size: 16g
    volumes:
      - /data/volumes-data/models/huggingface:/root/.cache/huggingface
      - /tmp:/tmp
    container_name: kyuz0-vllm

    tty: true
    restart: unless-stopped

    network_mode: "host"
    dns:
      - 1.1.1.1
      - 8.8.8.8

volumes:
  kyuz0-vllm:

