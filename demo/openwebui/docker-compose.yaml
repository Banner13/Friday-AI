include:
  - ../../compose-include.yaml

services:
  open-webui:
    container_name: ${OPENWEBUI_NAME?openwebui name not set!}
    image: ghcr.io/open-webui/open-webui:main
    environment:
      - OLLAMA_BASE_URL=http://${OLLAMA_NAME}:${OLLAMA_PORT}
      - OPENAI_API_BASE_URL=http://vllm:8000/v1
    volumes:
      - /data/volumes-data/app/open-webui:/app/backend/data
    networks:
      - ${SHARED_NETWORK?net issue.}
    ports:
      - "${OPENWEBUI_PORT:-8080}:${OPENWEBUI_HOSTPORT?port issue.}"

volumes:
  open-webui:
